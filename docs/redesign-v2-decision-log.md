# SDD ledger — plan: docs/superpowers/plans/2026-08-09-dai-design-alignment.md

Branch: design/dai-alignment
Baseline commit: 5676a3422ad9905dec1585b1f5e87d7065b30b8c
Target file: redesign-v2.html (single self-contained HTML page)

## Pre-flight scan (resolved by controller, no blocking conflicts)
- WorkCICO screenshot ownership is unstated in the plan. Ruling: implementer matches it to a log row by name; if no row matches, place it in the nearest project and attach a `needs-copy` chip asking the author to confirm. Never guess a project identity in prose.
- Task 1's hero thesis is controller-drafted copy, which sits in tension with the "do not invent author copy" constraint. Ruling: the constraint governs the 11 `.needs-copy` case-study chips; structural hero copy may ship as draft but must be flagged for author rewrite.
- `public/Black_RAiD__Reg_.png` exists but no task consumes it. Out of scope; not a conflict.
- Task 7 removes some Tailwind utility classes that Task 10 also targets. Overlap, not contradiction; Task 10 inventories whatever remains.

## Progress

Task 1: complete (commits 5676a34..cee6887, review clean)
Task 1: minor (deferred): the COUNTERS IIFE at redesign-v2.html:1592-1613 is now inert — it queries `[data-count]`, which Task 1 removed. Safe no-op (forEach on empty NodeList), but dead code plus a stale comment naming the deleted 20/2/35/3 stats. Candidate for Task 11 or final-review cleanup.
Task 1: minor (deferred): the brief claimed existing `.chip` styles to consume; no standalone `.chip{}` rule exists. Shipped CSS is self-contained, renders correctly. Brief inaccuracy, not a code defect.
Task 1: RESOLUTION for downstream tasks — the real section ids are `#wp1` (first featured case study) and `#record` (project log). `#featured`, `#log`, `#path` do NOT exist. Task 7's skip link must target `#wp1`, not `#featured`.

Task 2: implemented at commit 6b34f92. Review: spec ❌ (2 Important), image-to-project matching CONFIRMED CORRECT by an independent reviewer that opened all three images. Matches: facility-infographic → RSAF Facility Booking App (log ~992); WorkCICO → Workplace Check In/Out App (log ~1073); huawei-track-cert → Experience section, textually matches "National AI Student Challenge 2025 — Huawei Track" and the author's own name. No author-confirm chips needed.
Task 2: fix round 1/5 (2 addressed, 0 open — em-dash budget, alt-text quality; commits 6b34f92..0eb593a). Re-reviewer independently opened both images and confirmed the new alt text describes genuinely visible content, no fabrication.
Task 2: complete (commits cee6887..0eb593a, review clean)
Task 2: minor (deferred): the report's claim of live-testing the onerror fallback ("break a filename, confirm only the figure hides") is evidenced only by grep, not an actual browser run. Task 12 covers this properly.
Task 2: minor (deferred): facility-infographic alt at ~1011 opens with the product name "FacilityBook", structurally similar to the flagged pattern — but defensible, since "FacilityBook" is text printed on the infographic itself rather than a filename lift.
Task 6: fix round 1/5 (2 addressed, 0 open — `.band-cruise`/`.band-space` given unconditional `opacity:0`; `@media print` override `.invert,.invert *{background:none!important;color:var(--ink)!important}`; commits 66f5f42..f019852). Re-review recomputed both: flagged span now 4.67:1 worst case (passes), print text 18.5:1. All six light `.invert` colours confirmed covered by the override, and `.invert` has exactly two usages so the selector cannot miss a third.
Task 6: complete (commits 81cf4e5..f019852, review clean)
Task 6: minor (deferred): the broad `.invert *` print selector also strips `.btn-signal`'s orange fill and `.card`'s translucent panel/border in print. Cosmetic only, no contrast defect.
Task 6: NOTE FOR TASK 10 — the flagged span at ~647 uses the Tailwind class `text-muted`. With Tailwind removed, it inherits `--body` `#2C3E52` instead of `--muted` `#55697E`, which IMPROVES its no-JS contrast to ~10.3:1. Task 10 must confirm no element depends on a Tailwind colour class for a contrast pass.

Task 7: fix round 1/5 (2 addressed, 0 open; commits 91ce165..6afbbe1). Isolation measurement showed `scroll-padding-top:7rem` ALONE already gives 0.0px occlusion with 30.7px spare, so the added `scroll-margin-top` was genuinely redundant and was deleted along with its inaccurate comment. The mask fade was built, measured to zero out the last link's focus ring (24 accent px → 0), and correctly NOT shipped per instruction. Re-review confirmed Step 3's intent (zero occlusion) is satisfied by pre-existing CSS and that no JS scroll path bypasses `scroll-padding-top`.
Task 7: complete (commits f019852..6afbbe1, review clean)
Task 7: OBSERVATION — mobile nav scrollability still has no visual affordance. A CSS-only mask cannot work here because it clips the focus ring; a correct fix needs JS to suppress the mask at max scrollLeft. Out of scope for this plan; worth doing later.

Task 8: fix round 1/5 (3 addressed, 0 open; commits 1f71120..2ff6fb6). The deterministic pose-derivative test EARNED ITS COST: it found the first pass's ±1000ft breakpoint retune had moved the transit/waypoint seam off smoothstep's zero-velocity keyframes, producing a real ~20x second-derivative spike — a genuine stutter that screenshot-based "does it resolve" checking had masked. Fix reverted the breakpoints to the original leg boundaries and raised `.transit` to 27vh instead. Re-review independently derived the closed form (rate ∝ 1/h, predicted crossover 26.67vh) and confirmed the reported 1.48x/1.54x match it almost exactly — genuine measurement, not a number curve-fit to clear the bar.
Task 8: complete (commits 6afbbe1..2ff6fb6, review clean). Net effect: transits 40vh→27vh, two transits total 54vh (was 80vh), largest content gap 360px→243px against a 900px viewport. Smaller reclamation than the first attempt promised, but it is the honest consequence of the smoothness bar. The original defect (a full empty screenful) is resolved.
Task 8: OBSERVATION — the mobile override `@media (max-width:900px){.transit{height:15vh}}` was left untouched, so desktop (27vh) is now much closer to mobile (15vh) than 40vh was. Non-blocking.

Task 9: fix round 1/5 (5 addressed, 0 open; commits c82e335..fa2abb6). The disabled simulation pill now renders its own pre-existing `data-claim` text inline ("I can model a domain I wasn't trained in."), character-identical and unconditionally visible — no hover, no title attribute, no media gate. A reader can now see a scheduler does not belong to that category. `white-space:normal` scoped to `.pill:disabled` only, so the six enabled pills keep `nowrap`.
Task 9: complete (commits 2ff6fb6..fa2abb6, review clean). 24 rows; heading, `#logCount`, reset pill and meta description all agree; one pill still disabled and it genuinely has zero rows.

Task 10: fix round 1/5 (5 addressed, 0 open; commits 4e8a5d5..170fb96). `[hidden]{display:none}` restored — reviewer confirmed it wins on specificity (0,1,0 vs 0,0,1) and swept the file for later `display` re-assertions, finding only `.shot img` which never collides because those onerror handlers hide the parent `<figure>`, not the `<img>`. Stray `undefined/` binary deleted and gitignored with the root cause recorded. Both corrected ratios independently recomputed (12.28:1 / 6.85:1) and now agree with the block header. The extra unprefixed `text-size-adjust` was judged acceptable — real Preflight ships all three forms, so it is closer to Preflight, not a divergence.
Task 10: complete (commits fa2abb6..170fb96, review clean)
Task 10: minor (deferred): the `.invert` block header states "paper 7.9" for `#FFFFFF` on `#2A4E7A`; actual is ~8.50:1. Pre-existing, understated not overstated.

Task 12: fix round 1/5 (4 addressed, 0 open; commits 9814283..04ef8c7). `imgsBroken` now attaches load/error listeners before `<details>` are opened and before the lazy-load scroll, reading from `window.__imgTrack` rather than the post-hoc DOM. Re-reviewer independently reproduced the corrupt-image test in a scratch copy outside the repo: corrupted → `pass:false` with `resolvedVia:"error-event"`, restored → `pass:true`, `public/` untouched throughout. Missing anchor ids now gate the verdict via a `missingIds` field rather than reading as zero overlap. Contrast composites alpha; anchor check down from 18 pages to 2.
Task 12: complete (commits 6dcdcc5..04ef8c7, review clean)
ALL 12 TASKS COMPLETE. 24 commits on branch design/dai-alignment from baseline 5676a34 to 04ef8c7.

Task 12: implemented at commit 9814283. Review: 1 CRITICAL, 1 Important. All 7 checks reported PASS and every number was independently reproduced by the reviewer, including the detector JSON and the 49-chip count. But `imgsBroken` is a VACUOUS CHECK: it filters out images inside `[hidden]`, and every image on this page signals failure by setting `hidden=true` via onerror — so the one field designed to catch a broken image excludes exactly the state that indicates one. The reviewer proved it by corrupting an image in a scratch copy and getting `pass:true`. This is precisely the "silently passes because it measures nothing" outcome the task was written to guard against, and the implementer's own 3-bug self-audit missed it.
Task 12: the implementer found and fixed three real bugs in its own harness before trusting any number (a `URL` variable shadowing the global constructor that zeroed the network check, lazy images inside closed `<details>` never loading, and `scrollTo` racing `scroll-behavior:smooth`). All three verified genuine.
Task 12: CONTROLLER ACCEPTANCE — `em-dash-overuse` still fires at 6. Traced to Task 11's approved carve-outs: `<title>`, one project-name separator, and four date ranges. Not new prose. The detector's rule is literal and has no concept of a date range. Accepted, not suppressed.
Task 12: CONTROLLER ACCEPTANCE — the re-critique step is legitimately blocked. `/impeccable critique` gates on a `PRODUCT.md` this repo has never had, and writing one would mean deciding product strategy that is the author's call. A manual P0-by-P0 comparison against the original 20/40 snapshot was substituted rather than a fabricated score. Correct call; verified the gate is real.
Task 12: CHIP INVENTORY — 49 `.needs-copy` chips total (44 plain plus 5 with compound classes a naive grep misses). Home 1, Featured intro 1, FUEL Up 7, MILES/MAVIS 7, Masterclass 6, project log 27 across 17 rows. This is the author's outstanding to-do list and the single most useful artifact from this plan.

Task 11: complete (commits 170fb96..6dcdcc5, review clean, no fix round needed). Visible-copy em-dashes 17→1; eyebrows 6→2; `Learnt` 0; COUNTERS IIFE and `data-count` both gone with no dangling refs; `role="list"` on all three `.process` lists; "paper 7.9" corrected to 8.5 (independently recomputed at 8.499:1); inline SVG data-URI favicon with no network dependency; OG description verified claim-by-claim against the page.
Task 11: CONTROLLER CORRECTION — my instruction to delete the `.lab` CSS rule was WRONG, and the implementer correctly refused after verifying. `.lab` has 46 live call sites in the project-log grid; I confirmed this myself with grep before accepting the refusal. The bad instruction came from the Task 3 review, which reported `.lab` as orphaned — that reviewer saw only featured-card context, where Task 3 did delete nine `.lab` divs, and generalised wrongly to the whole file. Deleting it would have unstyled every project-card label. Lesson: a finding that an identifier is unused must be verified against the whole file, not a diff.
Task 11: minor (accepted): one em-dash remains in visible copy, in "National AI Student Challenge 2025 — Huawei Track", used as a title/subtitle separator. Under the ≤6 ceiling. My exemption named only date ranges, so this is a judgement-call extension; I accept it as correct typography for a title.

Task 10: implemented at commit 4e8a5d5. Review: 3 Important, 6 Minor, 0 Critical. 124 distinct Tailwind utilities replaced plus a hand-written Preflight-equivalent reset; CDN gone; residual-utility sweep found zero leftovers; naming confirmed semantic. New z-index scale: skip 30, nav 20, content 10, scene 2, sky 0, silo -1.
Task 10: MAJOR FIND — `.invert` was colliding with Tailwind's `invert` FILTER utility. `#contact` and `<footer>` were painting through `filter: invert(1)`, so the dark band rendered with inverted colours (navy as cream, signal orange as blue) while `color`/`background-color` computed correctly. Every contrast ratio Tasks 6 and 7 documented for that band described colours the browser was never showing. Only real screenshots could catch this; a computed-style diff could not. Removing the CDN fixes it. Post-fix dark-band pairs recomputed and all pass AA: body 12.27:1, headings 15.25:1, eyebrow 10.27:1, contact dt 8.20:1, signal button 6.49:1.
Task 10: SECOND FIND — a genuine contrast dependency on a Tailwind utility existed. `.lede` hard-codes `var(--body)` (~1.39:1 on the dark band); the contact lede was legible ONLY because of the arbitrary-value class `text-[#DCE8F4]`. Now carried by a real rule.
Task 10: CONTROLLER SIGN-OFF — the page-wide leading change is APPROVED. `body{line-height:1.65}` is the value this file declares; Preflight was beating it on source order, so the authored value had never rendered. Writing a rule to defeat the page's own stylesheet in order to preserve an artifact of a dependency being deleted would be the same dishonesty Step 4 exists to remove. Reviewer checked filter row, hero min-height, transits and nav clearance for breakage and found none. Cost: +3.6% document height. Accepted deliberately.
Task 10: minor (deferred): dark-band colour literals `#BFD4E8` now sit in the PAGE LAYOUT block rather than beside `.invert`; no new colours introduced, but the value appears three times.
Task 10: minor (deferred): `#catClaim{min-height:1.6rem}` is now inert at 1.65 leading.
Task 10: minor (deferred): `.hero-dot{border-radius:50%}` replaces `rounded-full`'s 9999px — identical on an 8x8 box, and matches `.tl-node`'s convention, but it was a free choice rather than a forced one.

Task 9: implemented at commit c82e335. Review: APPROVED, 2 Important, 6 Minor. Provenance verified sentence-by-sentence — every descriptive line in the three new rows traced to its case study, no invented assertion. 24 rows, heading/`#logCount`/reset pill/meta description all agree. "7 months" gone, experience order reverse-chronological by start date. Both "latent bug" fixes confirmed in-path and required, not scope creep.
Task 9: CONTROLLER RULING — the BRIEF was wrong, the implementer was right. Step 1 told it to tag the Flight Simulator Scheduling System as "Simulation & decision support". It refused, citing `src/data/portfolio.ts` (files `soar-scheduling` under Process Automation), the taxonomy spec's definition of category 1 ("the system reasons about a situation rather than recording transactions"), and the row's own slot-booking copy. Complying would have attached the pill's claim "I can model a domain I wasn't trained in" to a scheduler — manufacturing a capability claim, exactly what resolution 5 forbids. The brief line is struck; the code stands. Reviewer independently verified both sources.
Task 9: CONTROLLER RULING — the Huawei "inference" the implementer self-flagged is NOT unsupported. The reviewer opened `public/huawei-track-cert.jpg`; it visibly reads "National AI Student Challenge 2025 - Huawei Track" plus the author's name, a verbatim match to the row title. The association rests on the artifact, not the filename. Line stands.
Task 9: sent to fix round 1 — the reader-facing contradiction (disabled "Simulation — in preparation" pill ~130 lines above a "Flight Simulator Scheduling System" row) is still visible and the rename did not address it. Fix: render the category's existing claim string inline on the disabled pill so the reader can see a scheduler does not belong to it.
Task 9: minor (deferred): `<meta name="description">` count edit at redesign-v2.html:23 is outside the brief's named scope. Truthful; keeping it.
Task 9: minor (deferred): the new rows are titled with bare codenames "MILES / MAVIS" and "FUEL Up", which the taxonomy spec forbids (it requires a public descriptor with the codename demoted, and records codename clearance as Unresolved). Exposure is unchanged since both already appear as case-study `<h2>`s, and matching the case-study heading was right for the row — but the spec was cited selectively. This is author-copy territory; the `.context` chips already ask for plain descriptions.
Task 9: minor (deferred): three new outcome-label strings ("Outcome", "What it covered", "Who confirmed it") expand the log's previous label vocabulary in one commit. Defensible, worth a consistency look at the end.

Task 8: implemented at commit 1f71120. Review: 1 Important, 3 Minor. Mechanics verified correct — CAM/JET tables shifted in step, still ascending, no cockpit-keyframe collision, reduced-motion and print overrides untouched. Open finding: the motion-smoothness claim is evidenced only by "the pose completes and settles", which does not establish how it looks while playing. Sent back with a deterministic pose-derivative test against the 40vh baseline instead of relying on a human eyeball.
Task 8: PAGE LENGTH NOTE — document scrollHeight is 13,660px after this task, versus roughly 11,882px measured on the ORIGINAL page before any task ran. The page is now longer than it started despite reclaiming 416px of dead sky, because Tasks 1-5 added substantial real content (process spines, AI-role blocks, evidence figures, 26+ author chips). Trading empty sky for substance is the intended direction, but the final review should sanity-check total length against an admissions reader's patience.
Task 8: minor (deferred): `.context` renders at 1rem (16px), below the file's own documented 18px body-copy floor at redesign-v2.html:98. Pre-existing property of the reused class, now used in one more place.
Task 8: minor (deferred): the task report gives two unreconciled peak-rate figures (31.7 ft/px vs ~24 ft/px) for what appears to be the same quantity. Report-quality issue, not a code defect; folded into the fix round for reconciliation.

Task 7: implemented at commit 91ce165. Review: APPROVED, 1 Important (non-blocking), 3 Minor. Skip link is first in `<body>`, targets `#wp1` which exists, uses off-screen positioning so it stays in the accessibility tree. `hidden`/`md:flex` removed. `scroll-margin-top` on `section[id]` covers all six real anchor targets (`#ident`, `#origin`, `#wp1`, `#log`, `#record`, `#contact`).
Task 7: CONTROLLER RESOLUTION of the ⚠️ on Step 4 — the reviewer could not re-run the implementer's Playwright numbers. Not a gap: Task 12 builds the verification harness and re-measures the whole page. Deferred there rather than fixed here.
Task 7: minor (deferred): `.skip`'s `z-index:60` is a bare number coupled only by comment to the nav's Tailwind `z-50`. When Task 10 removes Tailwind and replaces `z-50`, 60 will not update itself. Task 10 must re-check this pairing.
Task 7: minor (deferred): `h2[id]` in the `scroll-margin-top` selector matches zero elements today. Inert, not wrong.
Task 7: OBSERVATION for final review — document-level horizontal overflow exists at 320px (356 vs 320), traced by git-stash comparison to pre-existing `.pill`/`<p>` content, NOT introduced by this task. Out of scope for Task 7; the final review should decide whether 320px support matters.

Task 6: implemented at commit 66f5f42. Review: 2 CRITICAL. Both are regressions the implementer introduced while fixing the original defect, and both were self-flagged as "cosmetic" — the reviewer disproved that with contrast arithmetic. (1) `.band{opacity:1}` default now lets the opaque `.band-space` gradient cover the whole sky with JS off, dropping the "Where I've worked" span at redesign-v2.html:647 from a documented 5.3:1 to 2.693:1 / 1.502:1. (2) The new print block never addressed `.invert`: browsers do not print backgrounds by default, so contact and footer print light-on-white. Confirmed clean by the same review: disabled-pill contrast 5.663:1 with no residual opacity, and the JS-on path safe because `Flight.subscribe` fires synchronously and sets inline opacity that outranks both rules.
Task 6: LESSON for remaining tasks — an implementer running Playwright and reporting PASS is not evidence the right thing was tested. Task 6's report claimed a verified print path while only checking that sky/scene/transit were hidden. Dispatches should name the specific measurement wanted, not ask for a pass/fail.

Task 5: fix round 1/5 (3 addressed, 0 open — `.log-body .context` specificity override, 5 chips capitalised, prescriptive chip made neutral; commits c1439bd..81cf4e5). Specificity verified independently: (0,2,0) beats (0,1,1), values copied verbatim from the base rule, no `!important`.
Task 5: complete (commits 2e5ed05..81cf4e5, review clean, 1 parked)

Task 5: implemented at commit c1439bd. Review: 2 Important, 0 Critical. THE ABSOLUTE HELD — zero `<abbr>` tags, zero `title=` attributes, no invented prose expansions; RAiD's expansion verified pre-existing in unchanged context. 7 chips added, 0 acronyms guessed. This was the highest invention-risk task in the plan and it came through clean.
Task 5: CONTROLLER RULING — PARKED, first-occurrence targeting (Important, plan-mandated). SSB, SAGE and 815 SQN first appear in always-visible `<summary class="lt">` row titles (redesign-v2.html:1106, 1224, 1252); the chips sit at the second occurrence in the collapsed body. Ruling: the code stands. The chip is an author-directed to-do marker rather than a reader-facing definition, so one chip per acronym does its whole job; a second chip inside the compact single-line summary grid would damage that layout while showing the author the same request twice; and filling the chip in removes the bare acronym from the title anyway. Real finding, deliberately not fixed. Final review sees both sides.
Task 5: minor (deferred → folded into fix round 1): chip capitalisation inconsistent with the file's "Add:" convention; one chip at ~1243 phrased prescriptively rather than as an open question.

Task 4: fix round 1/5 (5 addressed, 1 new Important opened — Lovable restored as a bare chip reopened Step 3's chip-purge guarantee; commits f801f4b..b6c1e93). Root cause: the controller offered two fix branches for Critical 1 and named the wrong one as simpler. The implementer had flagged the regression itself.
Task 4: fix round 2/5 (1 addressed, 0 open — Lovable moved out of the chip list into a `.needs-copy` question; commits b6c1e93..2e5ed05). Grep gate now has exactly one expected chip-list hit, the sanctioned `Copilot` in the SAGE row.
Task 4: complete (commits 8bef4c0..2e5ed05, review clean). Final state: 5 `.ai-role` blocks, 15 values — 6 page prose, 2 "Nothing. The product has no AI in it.", 1 adapted nothing-statement, 3 chips, 3 prose-plus-chip. Twelve chip-list deletions across eleven rows. Chips 25→26.

Task 4: implemented at commit f801f4b (base 8bef4c0; 8bef4c0 also carries a plan-file fix — the Task 4 heading was malformed as `---### Task 4:` and task-brief could not find it). Review: 1 Critical, 2 Important. Deletion pass verified complete: all 11 banned chip strings gone from every chip list.
Task 4: CONTROLLER RULING on Important 1 (labeled plan-mandated). My brief supplied the canned wording "Nothing. AI assisted the build, not the product." as an authorized answer. On FUEL Up and MILES / MAVIS that second sentence asserts build-assistance the page never claims — those two rows never named an AI tool anywhere, unlike Facility Booking and Flight Simulator whose chip lists did. The plan's absolute no-invented-facts constraint governs over my example wording. Departure authorized; the two-sentence form is reserved for rows where a chip or prose previously named an assistant.
Task 4: CONTROLLER RULING on Minor 1 (grep-gate exception, recorded). `<span>Copilot</span>` stays in the SAGE row chip list. The row's own summary reads "Integrating SAGE's ME5 Delta Agent INTO Copilot", making Copilot the host platform rather than a coding assistant; Step 3's deletion list names "GitHub Copilot", not "Copilot". Renaming it to disambiguate which Copilot product would itself invent a fact. Task 4's Step 5 grep gate therefore has one permanent expected hit.
Task 4: CONTROLLER DECISION — folded reviewer Minors 2 and 3 into the fix round despite the rule that minors do not enter the loop. Both are factual-accuracy defects in text THIS task authored (SAGE tense claiming a finished integration the source calls in-progress; "Nothing runs inside the training" contradicted by the page's own `.evi` line), i.e. the same defect class as the Critical, in blocks the implementer is already editing. Deferring known-false sentences would not serve the deliverable.
Task 4: minor (deferred): `.ai-role` grid `gap:.35rem` spaces dd-to-next-dt identically to within-pair spacing, so the three question/answer pairs do not read as pairs. Plan-mandated declarations. Token-free fix: `.ai-role dd+dt{margin-top:.4rem}`.
Task 4: minor (deferred): `.ai-role dd` has no measure cap while every sibling long-form element does (`.evi dd` 58ch, `.log-body p` 66ch, `.context` 60ch). Fix: `max-width:58ch`.
Task 4: minor (deferred): the page now has two conventions for the same dt/dd structure — `.ai-role` uses bare children, neighbouring `.evi` wraps each pair in a `<div>`. Valid either way; inconsistent.

Task 3: implemented at commit 0544482. Review: spec ❌ (3 Important), both ABSOLUTES HELD and were independently verified line-by-line — no case-study prose lost, nothing about the author invented, all 12 original chips byte-identical and unanswered, 4 new chips added (12→16).
Task 3: fix round 1/5 (3 addressed, 0 open — rubric gloss stripped from all 9 beats, person beat chipped on cards 1-2, framing beat chipped on cards 2-3; commits 0544482..7bcd211). Re-review confirmed no prose lost while stripping the gloss and no chip answered; chips 16→20, all 20 are author-directed questions.
Task 3: complete (commits 0eb593a..7bcd211, review clean)
Task 3: OBSERVATION for final review — the Iteration beat is chip-only on all three cards; the source material contains no iteration evidence anywhere. For a programme that teaches a "user-centred innovation process", this is the weakest beat on the page and the most valuable thing the author could write.
Task 3: CONTROLLER RULING on finding 1 (labeled plan-mandated). The brief's unbracketed gloss sentence was an instruction to the implementer, not shipped copy — the `[existing prose]` bracket convention marked the substitution slots. The plan's goal is a page demonstrating process, not one displaying its own worksheet, so stripping the gloss serves the plan. Authorized; no human adjudication required.
Task 3: CONTROLLER RULING on minor 1 (one sentence deleted rather than relocated, redesign-v2.html:674-676). The prose-preservation absolute protects CASE-STUDY prose — evidence about the author's work. The deleted line was structural meta-copy describing the old three-step card format that this task replaced; keeping it verbatim would leave a factually false sentence on the page. Deletion upheld as correct.
Task 3: minor (deferred): `.lab` CSS at ~194 is orphaned — its nine `<div class="lab">` consumers were deleted by this task. Cleanup for Task 11 or final review.
Task 3: minor (deferred): stale comment at ~814 still calls the Validation beat the "who confirmed it" slot.
Task 3: minor (deferred): `list-style:none` on the `.process` `<ol>` (~374) plus CSS-generated `::before` numbering drops list semantics in Safari/VoiceOver. INTRODUCED BY THIS TASK, not pre-existing. `role="list"` on the `<ol>` restores it at zero visual cost. Final review should triage this before merge.
Task 3: minor (deferred): the implementer's "30/30 verified by script" method is weaker than claimed — substring presence against a self-chosen inventory cannot detect deletion of an uninventoried line, and indeed missed :674. The reviewer's independent line-by-line trace reached the same conclusion, so the result holds; the method does not establish it.
Task 3: KNOWN RESIDUAL GAP (not fixable in this plan): the three FEATURED case studies still have zero imagery. Task 2's three assets matched log-row projects only; no artifact exists on disk for FUEL Up, MILES / MAVIS, or the Vibe Coding Masterclass. Critique P0-3 is therefore only partly closed. Surface this to the author — screenshots of the three flagship projects are the single highest-value asset they could add.

Task 2: CONTROLLER RULING on two findings labeled plan-mandated. The plan's Global Constraints section states it binds every task implicitly; the brief's example markup is illustrative, not binding. Therefore the em-dash cap and the alt-text-quality bar GOVERN over the brief's verbatim example strings, and departing from those examples is authorized rather than a plan contradiction. No human adjudication required — the plan's own text resolves the conflict. Findings sent to fix round 1.
